classdef (Abstract = true) AbstractSPGR < handle
    %ABSTRACTSPGR Abstract class to identify subclasses that share the SPGR
    %qMT pulse sequence properties (type of sequence for protocols, type of
    %fitted parameters, etc).
    %

    properties (Abstract = true)
    end

    methods (Abstract = true)
    end

end

